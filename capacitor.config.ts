import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.chitranjansb.anesthesiaassistant",
  appName: "Anesthesia Resident Assistant",
  webDir: "out",
};

export default config;
